#include <stdio.h>
#include <stdlib.h>
// Faça um programa que leia um número real e o imprima.
int main ()
{
   float numR;
   numR = 0;
   printf("\nEscreva um nomero real");
   scanf("%f", &numR);

   printf("\n Apresentar numero real: %f,numR");
system("pause");
return (0);
}