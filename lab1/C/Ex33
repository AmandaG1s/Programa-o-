#include <stdio.h>
//. Leia o tamanho do lado de um quadrado e imprima como resultado a sua área.

int main()
{
int lado, area;
printf("Escreva o lado de um  quadrado:");
scanf("%d",&lado);
area = lado *lado;
printf("Valor da area do quadrado:");
return 0;
}