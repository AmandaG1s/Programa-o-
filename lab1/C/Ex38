#include <stdio.h>
/* Leia o salário de um funcionário. Calcule e imprima o valor do novo salário, sabendo que ele
recebeu um aumento de 25%.*/
int main()
{
 float salario, aumento, valor_final;
  printf("Insira a valor do produto:");
  scanf("%f",&valor);
  desconto = valor * 0.12;
  valor_final = valor - desconto;
  
  printf("\nValor com o desconto sera: %.2f",valor_final);
return 0;
}